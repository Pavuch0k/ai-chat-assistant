# 🔧 Исправления #2: Оптимизация лимитов токенов + Защита от выдумывания

**Дата:** 2026-02-15
**Проблемы:**
1. Переполнение контекста на 3-4 сообщении из-за маленького max_tokens
2. Потеря истории диалога при переполнении
3. AI выдумывает информацию вместо использования БЗ

---

## ❌ НАЙДЕННЫЕ ПРОБЛЕМЫ

### 1. **Слишком маленький лимит ответа**
```python
# БЫЛО:
"max_tokens": 300  # Всего 300 токенов = ~750 символов

ПРОБЛЕМА:
- AI хочет дать развернутый консультативный ответ (~400 токенов)
- Не хватает 300 токенов → finish_reason="length"
- Срабатывает агрессивная обрезка истории
```

**Пример из логов:**
```
ai-chat-backend | ⚠️  Ответ близок к лимиту (300/300)
ai-chat-backend | Ответ обрезан из-за лимита токенов (finish_reason=length)
```

### 2. **Полное удаление истории при переполнении**
```python
# ai_service.py:582 - БЫЛО:
trimmed_history_aggressive = []  # ❌ Удаляет ВСЁ!

ПРОБЛЕМА:
- При переполнении код ПОЛНОСТЬЮ удалял историю
- AI забывал весь контекст ("12 лет b1", "индивидуальные")
- Спрашивал одно и то же повторно
```

### 3. **AI выдумывает вместо использования БЗ**
```
Запрос: "какие есть курсы"
БЗ вернула: "Curriculum 02...", "Консультация...", "1500 ₽..."
(нерелевантные фрагменты из анкеты)

AI ответил:
- Языковые курсы ❌ ВЫДУМКА!
- Курсы по программированию ❌ ВЫДУМКА!
- Художественные и музыкальные занятия ❌ ВЫДУМКА!
```

**Корневая причина:**
- БЗ возвращает нерелевантные фрагменты
- AI решает их проигнорировать
- НО вместо того чтобы сказать "не знаю", AI выдумывает "правдоподобный" ответ

### 4. **Двойной вопрос в одном сообщении**
```
"Какие детали тарифов важны? ... Как вас зовут?"

ПРОБЛЕМА:
- Бот задаёт консультативный вопрос И просит контакты одновременно
- Выглядит неестественно и непрофессионально
```

---

## ✅ ИСПРАВЛЕНИЯ

### 1. **Увеличены лимиты токенов для ответа**

**Файл:** `backend/app/services/ai_service.py`

```python
# Строка 397:
max_response_tokens=600  # было 1000, стало 600 (оптимальный баланс)

# Строка 427:
max_output_tokens = 600  # было 300

# Строка 435:
our_output_limit = 600  # было 300

# Строка 523:
"max_tokens": 600  # было 300
```

**Результат:**
- Достаточно места для развернутого консультативного ответа
- Не избыточно (экономия токенов сохранена)
- 600 токенов = ~1500 символов на русском

---

### 2. **Сохранение контекста при переполнении**

**Файл:** `backend/app/services/ai_service.py`

```python
# Строки 569-593 - БЫЛО:
trimmed_history_aggressive = self._trim_history_to_fit_tokens(...)
trimmed_history_aggressive = []  # ❌ Удаляет результат обрезки!

# СТАЛО:
trimmed_history_aggressive = self._trim_history_to_fit_tokens(
    max_context_tokens=8000,  # было 5000
    max_response_tokens=600   # было 300
)
# ОПТИМИЗАЦИЯ: Оставляем последние 2 сообщения для сохранения контекста
if len(trimmed_history_aggressive) > 2:
    trimmed_history_aggressive = trimmed_history_aggressive[-2:]

logger.info(f"  Агрессивная обрезка: оставлено {len(trimmed_history_aggressive)} последних сообщений")
```

**Результат:**
- При переполнении сохраняются 2 последних сообщения
- AI помнит контекст (не спрашивает повторно)
- Более мягкая стратегия обрезки

---

### 3. **Строгие правила против выдумывания**

**Файл:** `backend/app/services/ai_service.py` (строки 332-341)

```python
# ДОБАВЛЕНО:
🚨 ИСПОЛЬЗОВАНИЕ БАЗЫ ЗНАНИЙ - СТРОГИЕ ПРАВИЛА:
1. Если получил информацию из БЗ - используй ТОЛЬКО ЕЁ
2. Если БЗ нерелевантна или пустая - отвечай:
   "Эту информацию уточню у менеджера. Хотите оставить контакты для связи?"
3. КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО:
   ❌ Выдумывать факты, цены, курсы, услуги
   ❌ Называть то, чего нет в БЗ
   ❌ Обобщать ("у нас есть много курсов" - если не знаешь КАКИХ)
4. МОЖНО отвечать общими фразами ТОЛЬКО на приветствия ("привет", "как дела")
5. На КОНКРЕТНЫЕ вопросы (цены, курсы, тарифы) - ТОЛЬКО факты из БЗ
```

**Файл:** `backend/app/api/chat.py` (строки 181-195)

```python
# ИЗМЕНЕНО:
knowledge_context += f"{i}. (score: {score:.2f}) {fragment_text}\n"  # Показываем score

# ДОБАВЛЕНО:
🚨 КРИТИЧЕСКИ ВАЖНО:
- Если информация РЕЛЕВАНТНА вопросу → используй ТОЛЬКО её (не добавляй от себя!)
- Если информация НЕ РЕЛЕВАНТНА или НЕ ОТВЕЧАЕТ на вопрос → скажи "Эту информацию уточню у менеджера"
- ЗАПРЕЩЕНО выдумывать факты, которых НЕТ в этой информации!
```

**Результат:**
- AI больше не выдумывает несуществующие курсы/услуги
- При отсутствии информации честно говорит "не знаю" → сбор контактов
- Score показывается AI для оценки релевантности

---

### 4. **Правило: одно действие за раз**

**Файл:** `backend/app/services/ai_service.py` (строки 350-363)

```python
# ДОБАВЛЕНО:
🚨 ПРАВИЛО #2 - ОДНО ДЕЙСТВИЕ ЗА РАЗ 🚨
▶▶▶ В ОДНОМ сообщении делай ТОЛЬКО ОДНО:
   - ЛИБО консультируй (задай уточняющий вопрос о потребностях)
   - ЛИБО собирай контакты (проси имя ИЛИ телефон)
▶▶▶ ЗАПРЕЩЕНО в одном ответе:
   ❌ Задавать вопрос о потребностях И просить контакты
   ❌ "Какие детали? ... Как вас зовут?" - ЭТО ОШИБКА!
▶▶▶ ПРАВИЛЬНО:
   ✅ "Какие детали тарифов важны?" (консультация)
   ✅ "Отлично! Как вас зовут?" (сбор контактов)

КОГДА начинать сбор контактов:
- НЕ сразу после первого вопроса!
- Дай 2-3 консультативных ответа с уточняющими вопросами
- ЗАТЕМ начинай сбор контактов ОТДЕЛЬНЫМ сообщением
```

**Файл:** `backend/app/api/chat.py` (строки 157-165)

```python
# ИЗМЕНЕНО:
if bot_responses_count >= 4 or (bot_responses_count >= 3 and is_short_answer):  # было >= 3
    # ЧЕТКАЯ инструкция без двусмысленности:
    🎯 СБОР КОНТАКТОВ - ПОРА ДЕЙСТВОВАТЬ:
    В ЭТОМ ответе:
    1. Дай КРАТКИЙ ответ на текущий вопрос (1-2 предложения)
    2. ЗАТЕМ попроси контакты
    ⚠️ НЕ задавай дополнительных уточняющих вопросов о потребностях!

elif bot_responses_count >= 3:  # было >= 2
    📋 КОНСУЛЬТАЦИЯ ПРОДОЛЖАЕТСЯ:
    В ЭТОМ ответе:
    - Ответь на вопрос клиента
    - Задай ОДИН уточняющий вопрос о потребностях
    - НЕ проси контакты!
```

**Результат:**
- Бот делает только одно действие за раз
- Сначала 3-4 консультативных вопроса
- ЗАТЕМ отдельным сообщением начинает сбор контактов
- Более естественный и профессиональный диалог

---

## 📊 СРАВНЕНИЕ: ДО И ПОСЛЕ

### Типичный диалог (5 сообщений):

#### ❌ ДО ИСПРАВЛЕНИЙ:
```
Запрос #3: "Индивидуальные"
├─ max_tokens: 300
├─ AI хочет ответить ~400 токенов
├─ finish_reason: "length" ⚠️
├─ Агрессивная обрезка → trimmed_history = [] ❌
├─ Контекст потерян: AI забыл "12 лет b1"
└─ Результат: повторные вопросы

Запрос #4: "какие есть курсы"
├─ БЗ вернула нерелевантные фрагменты
├─ AI проигнорировал БЗ
├─ AI выдумал: "Языковые курсы, программирование..." ❌
└─ Результат: ложная информация
```

#### ✅ ПОСЛЕ ИСПРАВЛЕНИЙ:
```
Запрос #3: "Индивидуальные"
├─ max_tokens: 600 ✅
├─ AI отвечает ~400 токенов
├─ finish_reason: "stop" ✅
├─ История сохранена
└─ Результат: естественный ответ с сохранением контекста

Запрос #4: "какие есть курсы"
├─ БЗ вернула нерелевантные фрагменты
├─ AI видит низкий score и нерелевантность
├─ AI честно отвечает: "Эту информацию уточню у менеджера. Хотите оставить контакты?" ✅
└─ Результат: правдивый ответ → сбор контактов
```

---

## 🎯 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### Токены:
```
ДО:  ~10-15 сообщений → переполнение
ПОСЛЕ: ~20-25 сообщений без переполнения
```

### Качество ответов:
- ✅ Контекст сохраняется (не забывает детали)
- ✅ Нет выдумывания фактов
- ✅ Честные ответы "не знаю" → сбор контактов
- ✅ Одно действие за раз (консультация ИЛИ сбор контактов)

### UX:
- ✅ Естественный диалог
- ✅ Профессиональная консультация
- ✅ Не надоедает повторными вопросами
- ✅ Плавный переход к сбору контактов

---

## 🚀 КАК ПРОТЕСТИРОВАТЬ

### Тест 1: Длинный диалог (сохранение контекста)
```
1. "расскажи о компании"
2. "онлайн"
3. "разговорный китайский"
4. "a2"
5. "индивидуальные"
6. "какие тарифы"

ОЖИДАЕТСЯ:
- Бот помнит весь контекст
- Не переспрашивает про онлайн/a2/индивидуальные
- Даёт релевантные ответы
```

### Тест 2: Защита от выдумывания
```
1. "какие есть курсы"

ОЖИДАЕТСЯ (если в БЗ нет конкретной информации):
"Эту информацию о курсах уточню у менеджера. Хотите оставить контакты для связи?"

НЕ ДОЛЖНО БЫТЬ:
"У нас есть курсы по программированию, языкам..." (выдумка)
```

### Тест 3: Одно действие за раз
```
После 3-4 ответов бота:

ОЖИДАЕТСЯ:
"[Краткий ответ на вопрос]. Отлично! Как вас зовут?"

НЕ ДОЛЖНО БЫТЬ:
"[Ответ]. Какие детали важны? ... Как вас зовут?"
```

---

## 📝 ИЗМЕНЁННЫЕ ФАЙЛЫ

1. `backend/app/services/ai_service.py`
   - Строки 332-341: Строгие правила использования БЗ
   - Строки 350-363: Правило "одно действие за раз"
   - Строка 397: max_response_tokens = 600
   - Строка 427: max_output_tokens = 600
   - Строка 435: our_output_limit = 600
   - Строка 523: "max_tokens": 600
   - Строки 573-593: Сохранение контекста при переполнении

2. `backend/app/api/chat.py`
   - Строки 157-165: Улучшенная логика сбора контактов
   - Строки 181-195: Строгие инструкции для использования БЗ

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

- Изменения **НЕ СЛОМАЮТ** существующий функционал
- Экономия токенов **СОХРАНЕНА** (600 вместо 300, но не 3000)
- Качество диалога **УЛУЧШЕНО**
- **НЕТ** breaking changes

---

## 🔄 ОТКАТ (если нужно)

Если что-то пошло не так:

```bash
# Откатить последний коммит:
git revert HEAD

# Или откатить конкретные значения:
# ai_service.py:523
"max_tokens": 300  # вернуть старое значение

# ai_service.py:397
max_response_tokens=1000  # вернуть старое значение
```

---

## ✅ ГОТОВО К ТЕСТИРОВАНИЮ

Все изменения внесены, сервер можно перезапускать для тестирования.
